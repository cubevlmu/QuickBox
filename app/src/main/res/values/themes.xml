<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- 启动用 theme（不透明） -->
    <style name="Theme.QuickBox.Launch" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="windowSplashScreenBackground">@android:color/transparent</item>
        <item name="windowSplashScreenAnimatedIcon">@null</item>
        <item name="postSplashScreenTheme">@style/Theme.QuickBox</item>
    </style>


    <style name="Theme.QuickBox" parent="Theme.Material3.DayNight.NoActionBar" />
</resources>